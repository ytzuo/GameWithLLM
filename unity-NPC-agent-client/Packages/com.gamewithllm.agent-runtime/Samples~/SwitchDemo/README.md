# Switch Demo

This sample shows that the SDK is not tied to NPCs: an interactive building
switch implements `IAgentEntity`, and its tool implements `IAgentTool`.
The sample intentionally contains no A2A, MCP, Gateway, or Go DTO references.
